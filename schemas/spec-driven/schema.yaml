name: spec-driven
version: 1
description: Default OfficeSpec workflow - proposal → specs → plan → tasks (for any office work)
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      Create the proposal document that establishes WHY this change is needed.
      Works for any office work: operations, HR, finance, marketing, legal,
      admin, customer support, or software.

      Sections:
      - **Why**: 1-2 sentences on the problem or opportunity. What problem does this solve? Why now?
      - **What Changes**: Bullet list of changes. Be specific about new capabilities, modifications, or removals. Mark breaking changes with **BREAKING**.
      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<capability-path>/spec.md`. Use kebab-case for path segments you introduce (e.g., `expense-approvals` or `finance/expense-approvals`) and follow the team's existing spec organization.
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level expectations change (not just how the work is done). Each needs a delta spec file. Use the exact existing path under `openspec/specs/`. Leave empty if no requirement changes.
      - **Impact**: Affected teams, docs, tools, processes, or systems.

      IMPORTANT: The Capabilities section is critical. It creates the contract between
      proposal and specs phases. Research existing specs before filling this in:
      run `openspec list --specs` for the team's capability inventory, then
      `openspec show "<spec-id>" --type spec --json --no-scenarios` for any that
      look related - that returns a capability's purpose and requirement texts
      without pulling whole spec files into context. Append `--store "<id>"` to
      both commands only for a registered standalone store, and keep `--type
      spec`: a change and a spec sharing a name is otherwise an ambiguous-item
      error. `openspec list` without `--specs` lists in-flight changes, not
      specs - it never shows what the team already covers. Reuse an existing
      capability's exact path instead of introducing a near-duplicate name.
      The filtered read is only an overview. Before deciding what is already
      covered or what should change, read each relevant spec in full, including
      scenarios, with `openspec show "<spec-id>" --type spec` (same `--store` rule).
      Each capability listed here will need a corresponding spec file.

      Every change must either declare at least one capability (new or
      modified) or explicitly opt out of specs: `openspec validate` rejects a
      change with zero deltas unless the change's `.openspec.yaml` sets
      `skip_specs: true`. Use `skip_specs: true` only when no spec-level
      expectations change (minor process tweak, tooling, docs) - specs describe
      outcomes, so if outcomes do not change, no spec should change either.
      Do not invent a requirement just to satisfy validation.

      Keep it concise (1-2 pages). Focus on the "why" not the "how" -
      approach details belong in design.md.

      This is the foundation - specs, design, and tasks all build on this.
    requires: []

  - id: specs
    generates: "specs/**/*.md"
    description: Detailed specifications for the change
    template: spec.md
    instruction: |
      Create specification files that define WHAT the work should achieve.

      A spec is an outcome contract, not a step-by-step plan.

      Good spec content:
      - Observable outcomes people or downstream teams rely on
      - Inputs, outputs, and outcomes (including error and exception cases)
      - External constraints (compliance, privacy, quality, deadlines, compatibility)
      - Scenarios that can be checked or explicitly validated

      Avoid in specs:
      - Internal tool, template, or file names
      - Vendor or software choices
      - Step-by-step execution details
      - Detailed work plans (those belong in design.md or tasks.md)

      Quick test: if the delivery approach can change without changing the
      externally visible outcome, it likely does not belong in the spec.

      Create one spec file per capability listed in the proposal's Capabilities section.
      `<capability-path>` is the spec directory relative to `specs/` (for example,
      `expense-approvals` or `finance/expense-approvals`). Preserve the full path:
      - New capabilities: use the exact path from the proposal at `specs/<capability-path>/spec.md`. Any path segment newly introduced in the proposal must be kebab-case. Follow the team's existing organization; do not add a new domain level when the team uses a flat layout.
      - Modified capabilities: use the exact existing path from `openspec/specs/<capability-path>/` when creating the delta at `specs/<capability-path>/spec.md`. Run `openspec list --specs` to confirm that path before writing the delta, appending `--store "<id>"` only for a registered standalone store - a mistyped or invented path targets a capability that does not exist rather than the one you meant. Do not move or rename the capability.

      There must be at least one spec file unless the change's `.openspec.yaml`
      sets `skip_specs: true` (no spec-level outcome change) - `openspec validate`
      rejects a zero-delta change without that marker. If the proposal lists no
      capabilities and `skip_specs` is not set, revisit the proposal first.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed expectations - MUST include full updated content
      - **REMOVED Requirements**: Retired expectations - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.

      New capabilities only: start the delta spec with a `## Purpose` section -
      one or two sentences (50+ characters, or `openspec validate --strict`
      reports it as too brief) describing what the capability is for. Archive
      copies it into the main spec it creates; without it the new main spec is
      left with a `TBD ... Update Purpose after archive` placeholder to fill in
      by hand. Do NOT add `## Purpose` to a delta for an existing capability -
      that spec already has one and the delta's is ignored. To change an
      existing capability's Purpose - including a leftover `TBD` placeholder -
      edit `<planningHome.root>/openspec/specs/<capability-path>/spec.md`
      directly. `planningHome.root` comes from the `openspec instructions ...
      --json` response. Always use it rather than a folder-relative path: it
      resolves to the store whenever the change lives in one - whether that
      came from `--store`, a team `store:` pointer, or a global default
      store - and to the current folder otherwise. Do not try to work out
      which case applies; the field already has.

      MODIFIED requirements workflow:
      1. Locate the existing requirement in `<planningHome.root>/openspec/specs/<capability-path>/spec.md` (the same store-aware root as above)
      2. Copy the ENTIRE requirement block (from `### Requirement:` through all scenarios)
      3. Paste under `## MODIFIED Requirements` and edit to reflect new expectations
      4. Ensure header text matches exactly (whitespace-insensitive)

      Common pitfall: Using MODIFIED with partial content loses detail at archive time.
      If adding new concerns without changing existing expectations, use ADDED instead.

      Example (a new capability, so it opens with `## Purpose`):
      ```
      ## Purpose

      Lets team members get expenses approved quickly with full visibility.

      ## ADDED Requirements

      ### Requirement: Team members can track expense requests
      The process SHALL let team members submit expense requests and see their status at any time.

      #### Scenario: Successful submission
      - **WHEN** a team member submits an expense request
      - **THEN** the request is routed for approval and its status is visible to the requester

      ## REMOVED Requirements

      ### Requirement: Email-only approvals
      **Reason**: Replaced by tracked approval process
      **Migration**: Submit new requests with the standard expense form
      ```

      Specs should be checkable - each scenario is a potential acceptance check.
    requires:
      - proposal

  - id: design
    generates: design.md
    description: Approach and plan document with delivery details
    template: design.md
    instruction: |
      Create the design document that explains HOW to carry out the change.
      For office work, read "design" as "approach and plan" - no technical
      background needed. Include technical details only when the work itself
      is technical.

      When to include design.md (create only if any apply):
      - Cross-cutting change (multiple teams or processes) or new way of working
      - New external vendor, tool, or significant process or data changes
      - Compliance, quality, or rollout complexity
      - Ambiguity that benefits from decisions before doing the work

      Sections:
      - **Context**: Only the current state and constraints needed to explain the approach. Reference the proposal for motivation instead of restating it (e.g., "See proposal.md - Why").
      - **Goals / Non-Goals**: What this plan achieves and explicitly excludes. Don't restate the proposal's scope - add only plan-level boundaries.
      - **Decisions**: Key choices with rationale (why X over Y?). Include alternatives considered for each decision.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Rollout Plan**: Steps to launch, who does what, rollback or fallback (if applicable)
      - **Open Questions**: Unknowns that can safely be answered later without
        changing the specs, the approach, or the task breakdown. Omit if none.

      Open questions are for genuinely deferrable unknowns, not decisions you
      skipped. If a question would change the specs, the chosen approach, or
      the task breakdown, resolve it now - ask the user instead of guessing.

      Focus on approach and coordination, not step-by-step execution.
      The proposal covers why and what; design covers how. Reference the
      proposal for motivation and, once written, the specs for requirements -
      if a section would only restate them, point to them instead.

      Good plan docs explain the "why" behind choices.
    requires:
      - proposal

  - id: tasks
    generates: tasks.md
    description: Action checklist with trackable tasks
    template: tasks.md
    instruction: |
      Create the task list that breaks down the work.

      Before writing tasks, check design.md for Open Questions. If any of them
      would change what gets delivered, resolve them with the user first - do not
      bake an unstated assumption into the task list.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses
      checkbox format to track progress. Tasks not using `- [ ]` won't be tracked.

      Guidelines:
      - Group related tasks under ## numbered headings
      - Each task MUST be a checkbox: `- [ ] X.Y Task description`
      - Tasks should be small enough to complete in one session
      - Order tasks by dependency (what must be done first?)
      - Each task MUST state how to verify completion (a check, review,
        observable outcome, or delivered artifact). Put the verification in
        that task's checkbox description. Use a separate verification task only
        when it checks broader outcomes that span multiple tasks.

      Example:
      ```
      ## 1. Prepare

      - [ ] 1.1 Draft new request form and verify the team lead approves the fields
      - [ ] 1.2 Update the team handbook and verify the new steps are documented

      ## 2. Roll out

      - [ ] 2.1 Share the new process with the team and verify everyone confirms receipt
      - [ ] 2.2 Run a trial week and verify requests complete within the target time
      ```

      Reference specs for what needs to be delivered, design for how to deliver it.
    requires:
      - specs
      - design

apply:
  requires: [tasks]
  tracks: tasks.md
  instruction: |
    Read context files, work through pending tasks, mark complete as you go.
    Pause if you hit blockers or need clarification.
